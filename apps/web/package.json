{
  "name": "@kronos/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx serve src -l 3000",
    "build": "mkdir -p ../../dist && cp -r src/* ../../dist/ && cp -r src/* . && echo 'build platinum OK'"
  }
}
